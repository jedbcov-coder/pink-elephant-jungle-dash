{
  "name": "pink-elephant-jungle-dash",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "npm run check:css && tsc -p tsconfig.json --noEmit",
    "test": "npm run check",
    "lint": "npm run check",
    "build:pages": "vite build --outDir docs",
    "check:css": "node scripts/check-css-braces.mjs"
  },
  "dependencies": {
    "react": "19.2.6",
    "react-dom": "19.2.6",
    "three": "0.184.0",
    "vite": "8.0.12"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
